###################################
# GENERAL SETTINGS

setvar! average_ping 200
setvar! movement_delay 150

# should script use runetomes to travel to mine
setvar! auto_cycle_runetomes 0
# how many runes script should attempt to recall for each runebook
setvar! max_runes_to_use_per_runebook 26

# set to 1 to activate void armor on start
setvar! use_harvest_aspect 0
setvar! use_void_aspect 0

# should script recall home when tracking pks
setvar! auto_escape_pks 1
setvar! auto_escape_thiefs 0

# max resources a packie can have before being set as full
setvar! max_resources_in_packie_before_unloading 1000

# this requires you to run before my loot-organizer script to set the variable CHAR_LOOT_POUCH
setvar! hide_colored_resources_in_char_loot_pouch 1

# should script try to enter hotel room when traveling home
setvar! use_hotel_room 0
# set if characters should use own rooms or enter a friends room
setvar! use_own_room 0


#############################
# CHARACTER SETTING OVERRIDES

if name == 'Emacs'
    setvar! auto_cycle_runetomes 1
    setvar! use_void_aspect 1
endif

if name == 'nonato'
    setvar! auto_cycle_runetomes 1
    setvar! use_void_aspect 1
endif

if name == 'Nazareno'
    setvar! auto_cycle_runetomes 1
    setvar! max_runes_to_use_per_runebook 10
    setvar! use_void_aspect 1
endif


#############################
# CHARACTER SETTING OVERRIDES


sysmsg '> LUMBER CONFIG LOADED!' 75
overhead '*lumber config loaded!*' 75

setvar! lumber_config_loaded 1
script 'resources\lumber\lumber'
