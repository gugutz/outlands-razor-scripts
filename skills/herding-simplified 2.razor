say 'all kill'
wft 500
if targetexists 'harmful'
    while targetexists 'harmful'
        wait 50
    endwhile
    wait 100
endif

if not dead 'last' or not find 'last' ground any any 20
    if findtype 3713 as crook
        useobject crook
        wft 600
        if targetexists 'harmful'
            hotkey 'Last Target'
            wait 400
            if insysmsg 'You focus your follower'
                overhead 'focused pets agression' 41 'last'
            endif
        endif
    else
        overhead "no sheperd's crook!"
    endif
endif
