setvar! action_delay 300

if followers > 0
    say '[disembarkfollowers'
    wft action_delay
    if targetexists 'neutral'
        while targetexists 'neutral'
            overhead 'where to disermbark followers?' 53
        endwhile
    endif
endif

say '[disembark'

wft action_delay

if targetexists 'neutral'
    while targetexists 'neutral'
        overhead 'where to disermbark?' 53
    endwhile
endif
