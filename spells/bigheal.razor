if targetexists 'beneficial'
    if hits < maxhits
        target 'self'
    else
        overhead 'holding target (100% hp)' 41
    endif
else
    hotkey 'Attack Last Target'
    cast 'greater heal'
endif
