
hotkey 'Toggle Peace Only'

if targetexists 'beneficial'
    if poisoned or hits < maxhits
        hotkey 'Target Self'
    else
        overhead '** hp 100% **' 340 'self'
    endif
else
    hotkey 'Play: spells\heal'
endif