#if not hidden
#    if findtype 292 ground any any 2 as llama
#        cast 'invisibility'
#        wft 3500
#        target llama
#        wait 200
#    endif
#    cast 'invisibility'
#    wft 3500
#    hotkey 'Target Self'
#    wait 200
#else
if diffhits > 20
    cast 'greater heal'
    wft 3500
    hotkey 'Target Self'
elseif diffhits > 0
    cast 'heal'
    wft 1500
    hotkey 'Target Self'
else
    hotkey 'Clear Scavenger Cache'
    wait 200
    hotkey 'Organizer Agent-01'
    wait 2000
    while queued
        overhead 'Moving gold to llama'
        wait 200
    endwhile
endif
loop
#endif
#loop